class Node:
	def __init__(self, contents = None, next = None):
		self.contents = contents
		self.next = next
	def get_contents(self):
		return self.contents

class List:
	def __init__(data):
		self.head_node.contents = data
		self.head_node.next = None
		self.cur_node = head_node

	def append_node(contents):
		cu
		

	def print_list(list):
		node = list.




def test_list():
	node = Node(1)
	node.next = Node(2)
	node.next.next = Node(3)
	node.print_list()
